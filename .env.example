# Firebase Auth — when set, enables Firebase authentication instead of password gate.
# Leave unset for local dev to use the simple password gate.
# Get these values from Firebase Console → Project Settings → General → Your Apps
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Optional: Connect to Firebase emulators in dev mode
# VITE_FIREBASE_EMULATOR=true

# Cloudflare Analytics (production only)
# VITE_CF_ANALYTICS_TOKEN=
